# NOFX 环境配置文件
# 请根据实际情况修改以下值

# ===== 安全配置 =====
# JWT 密钥，用于生成和验证 Token
# 生产环境请使用强密钥（至少 32 个字符）
JWT_SECRET=change-me-to-secure-random-key-at-least-32-chars

# 数据加密密钥
# 用于加密敏感数据
DATA_ENCRYPTION_KEY=change-me-to-secure-random-key-for-encryption

# RSA 私钥（可选）
# 用于高级加密操作
RSA_PRIVATE_KEY=

# ===== 数据库配置 =====
# 数据库类型: sqlite（推荐离线使用）
DB_TYPE=sqlite

# 数据库路径
# 相对路径会相对于 docker-compose.yml 所在目录
DB_PATH=/app/data/data.db

# ===== 应用配置 =====
# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# 时区
TZ=Asia/Shanghai

# ===== 前端配置 =====
# 前端 API 地址
# 如果前端和后端在同一主机上，使用 http://localhost:8080
REACT_APP_API_URL=http://localhost:8080

# ===== 端口配置 =====
# 后端端口（在 docker-compose.yml 中配置）
# BACKEND_PORT=8080

# 前端端口（在 docker-compose.yml 中配置）
# FRONTEND_PORT=3000

# ===== 可选配置 =====
# 在此添加其他环境变量
